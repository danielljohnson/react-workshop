var RemoveCompletedTasksButton = React.createClass({
    render: function() {
        return (
            <button type="button" className="btn btn-primary">Remove Completed</button>
        );
    }
});

export default RemoveCompletedTasksButton;